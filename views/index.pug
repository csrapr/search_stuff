extends layout

block content
  .container
    h1 Leisure Search
    form(method="GET" action="/search")
      .form-group
        label(for='searchBox') Search Activities
        input#searchBox.form-control(type='text' placeholder='Search term' name="q")
      .form-group
        input.btn.btn-success(type="submit" value="Search now")

